name: pr

on: [pull_request]

jobs:
  build:
    uses: wittdennis/.github/.github/workflows/build-docker-image.yaml@0.1.0
    with:
      image-name: homeassistant-otbr
      create-artifacts: false
      arm64-build-args: |
        ARCH=aarch64
