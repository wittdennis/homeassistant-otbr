name: pr

on: [pull_request]

jobs:
  build:
    uses: wittdennis/pipelines/.github/workflows/container-image-build.yaml@d30ae383896657cd09cf53e41b71edbe06a58a6d # 2.0.17
    with:
      image-name: homeassistant-otbr
      create-artifacts: false
      arm64-build-args: |
        ARCH=aarch64
